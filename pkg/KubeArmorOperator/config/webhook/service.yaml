apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: kubearmor
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    kubearmor-app: kubearmor-operator