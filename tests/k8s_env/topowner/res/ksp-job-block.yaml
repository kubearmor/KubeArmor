apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: ksp-job-block
  namespace: ksp-test
spec:
  severity: 5
  selector:
    matchLabels:
      app: samplejob
  file:
    matchPaths:
      - path: /etc/hosts
  action:
    Block
