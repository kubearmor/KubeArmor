apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: block-dns-query-to-google-subdomains
  namespace: multiubuntu
spec:
  severity: 10
  selector:
    matchLabels:
      container: ubuntu-1
  network:
    matchDNSQueries:
    - domain: google.com
  action: Block
