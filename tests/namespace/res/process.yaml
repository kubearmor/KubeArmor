apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: process-allow-curl
  namespace: nptest
spec:
  severity: 8
  selector:
    matchLabels:
      container: ubuntu-1
  process:
    matchPaths:
    - path: /usr/bin/curl
  action:
    Allow
