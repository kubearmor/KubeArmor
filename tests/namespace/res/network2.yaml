apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: network-block-udp
  namespace: nptest
spec:
  severity: 8
  selector:
    matchLabels:
      container: ubuntu-1
  network:
    matchProtocols:
    - protocol: udp
      fromSource:
      - path: /usr/bin/curl
  action:
    Block
