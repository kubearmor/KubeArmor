apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: process-block-ls
  namespace: nptest
spec:
  severity: 8
  selector:
    matchLabels:
      container: ubuntu-1
  process:
    matchPaths:
    - path: /bin/ls
  action:
    Block
