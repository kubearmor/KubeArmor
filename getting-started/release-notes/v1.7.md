# KubeArmor v1.7 Release Notes

## 🆕 New Features

- Added support for IPv6 host addresses so that environments using IPv6 or dual-stack networking can be monitored and protected alongside IPv4 setups.
- Introduced configurable resource settings in the KubeArmor Helm charts so operators can tune CPU and memory usage for core components during deployment.

## 🐛 Bug Fixes

- Improved process monitoring so that missing parent process names on the host are correctly resolved and reported in visibility data.
- Corrected several small issues in project configuration, documentation, and tests so that the overall experience is more reliable and polished for users and contributors.

## 🎁 Misc

- Marked version v1.6.5 as the stable release and updated release metadata so users can clearly see which version is recommended for production use.
- Updated the KubeArmor Helm charts and default values so that installations use version-aligned images and more complete configuration out of the box.
- Enhanced preset security alerts so that built-in alerts include all expected informational fields and provide clearer context.
- Documented DNS visibility capabilities so users can learn how to enable and interpret DNS-level observability in their clusters.
- Polished user-facing log messages and release documentation so operational information is clearer and more professional.

